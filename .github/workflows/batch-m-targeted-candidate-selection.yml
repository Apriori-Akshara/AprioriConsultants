name: Batch M targeted candidate selection

on:
  push:
    branches:
      - batch-m-rw-generator-remediation-2026-09-14
    paths-ignore:
      - docs/BATCH-M-TARGETED-CANDIDATE-SELECTION-2026-09-15.json
      - package-lock.json
  workflow_dispatch:

permissions:
  contents: write

jobs:
  candidate-selection:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout remediation branch
        uses: actions/checkout@v4
        with:
          ref: batch-m-rw-generator-remediation-2026-09-14
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Repair and validate package lock
        run: |
          npm install --package-lock-only --ignore-scripts --no-audit --no-fund
          npm ci --ignore-scripts --no-audit --no-fund

      - name: Run exact replacement-candidate selection
        run: npm run qc:batch-m-targeted-candidate-selection

      - name: Verify candidate-selection report
        run: |
          test -s docs/BATCH-M-TARGETED-CANDIDATE-SELECTION-2026-09-15.json
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const report = JSON.parse(fs.readFileSync('docs/BATCH-M-TARGETED-CANDIDATE-SELECTION-2026-09-15.json', 'utf8'));
          if (report.productionMutation !== false) throw new Error('productionMutation must be false');
          if (report.releaseEligible !== false) throw new Error('releaseEligible must be false');
          if (report.replacementAuthorization !== 'NOT_AUTHORIZED') throw new Error('replacementAuthorization must remain NOT_AUTHORIZED');
          if (report.sat21Created !== false) throw new Error('sat21Created must be false');
          if (report.affectedUniqueQuestionCount !== 2144) throw new Error(`Expected 2144 affected records, got ${report.affectedUniqueQuestionCount}`);
          if (!report.summary || typeof report.summary.selected !== 'number' || typeof report.summary.noEligibleCandidate !== 'number') {
            throw new Error('Candidate-selection summary is missing required counts');
          }
          if (!Array.isArray(report.records) || report.records.length !== 2144) {
            throw new Error(`Expected 2144 report records, got ${report.records?.length ?? 'missing'}`);
          }
          console.log(JSON.stringify({
            affectedUniqueQuestionCount: report.affectedUniqueQuestionCount,
            selectedCount: report.summary.selected,
            noEligibleCandidateCount: report.summary.noEligibleCandidate,
            productionMutation: report.productionMutation,
            releaseEligible: report.releaseEligible,
            replacementAuthorization: report.replacementAuthorization,
            sat21Created: report.sat21Created
          }, null, 2));
          NODE

      - name: Commit generated report and repaired lockfile
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json docs/BATCH-M-TARGETED-CANDIDATE-SELECTION-2026-09-15.json
          if git diff --cached --quiet; then
            echo "No generated changes to commit"
          else
            git commit -m "chore: record Batch M candidate selection and sync lockfile"
            git push origin HEAD:batch-m-rw-generator-remediation-2026-09-14
          fi
